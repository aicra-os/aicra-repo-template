{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Contribution Plan Schema",
  "type": "object",
  "required": ["intent", "author", "context_version", "constraints", "expected_output"],
  "properties": {
    "intent": { "type": "string" },
    "author": { "type": "string" },
    "context_version": { "type": "string" },
    "constraints": { "type": "array", "items": { "type": "string" } },
    "expected_output": { "type": "array", "items": { "type": "string" } }
  }
}
